class Solution:
    def numDistinct(self, s: str, t: str) -> int:
        m,n=len(s),len(t)
        k=[0]*(n+1)
        k[0]=1
        for j in s:
            for i in range(n,0,-1):
                if t[i-1]==j:
                    k[i]+=k[i-1]
        return k[n]
