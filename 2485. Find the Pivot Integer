class Solution:
    def pivotInteger(self, n: int) -> int:
        m=(n*(n+1))/2
        for i in range(1,n+1):
            h=(i*(i+1))/2

            j=(i*(i-1))/2
            if m-j==h:
                return i
        return -1
