class Solution:
    def checkOverlap(self, r: int, x: int, y: int, x1: int, y1: int, x2: int, y2: int) -> bool:
        m=max(x1,min(x,x2))
        n=max(y1,min(y,y2))
        i=x-m
        j= y-n
        return (i**2+j**2)<=(r**2)
