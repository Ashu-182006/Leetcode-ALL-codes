class Solution:
    def winnerSquareGame(self, n: int) -> bool:
        d=[False]*(n+1)
        for i in range(1,n+1):
            for j in range(1,int(i**0.5)+1):
                if not d[i-j*j]:
                    d[i]=True
                    break
        return d[n]
